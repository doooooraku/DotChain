diff --git a/.github/workflows/build-ios-device.yml b/.github/workflows/build-ios-device.yml
index 9407acc..b69c8a9 100644
--- a/.github/workflows/build-ios-device.yml
+++ b/.github/workflows/build-ios-device.yml
@@ -31,30 +31,7 @@ jobs:
       - name: 📚 ライブラリをインストール
         run: pnpm install
 
-      # 3. 【重要】環境変数の作成
-      # これがないと、アプリの中にAPIキーが含まれず、起動してもエラーになります
-      - name: 🔎 RevenueCatキーの存在チェック
-        env:
-          REVENUECAT_IOS_API_KEY: ${{ secrets.REVENUECAT_IOS_API_KEY }}
-        run: |
-          if [ -z "$REVENUECAT_IOS_API_KEY" ]; then
-            echo "REVENUECAT_IOS_API_KEY is empty"
-            exit 1
-          fi
-
-      - name: 🔓 .envファイルを作成
-        env:
-          REVENUECAT_IOS_API_KEY: ${{ secrets.REVENUECAT_IOS_API_KEY }}
-          IAP_DEBUG: ${{ secrets.IAP_DEBUG }}
-        run: |
-          # 必要なキーをここに追加してください
-          echo "REVENUECAT_IOS_API_KEY=${REVENUECAT_IOS_API_KEY}" > .env
-          if [ -n "$IAP_DEBUG" ]; then
-            echo "IAP_DEBUG=${IAP_DEBUG}" >> .env
-          fi
-        # 注意: GitHubのSecretsにこれらのキーを登録しておく必要があります
-
-      # 4. ビルド（IPA作成）
+      # 3. ビルド（IPA作成）
       # --local: GitHubのMacで作る（無料）
       # --profile production: eas.jsonの "store" 設定を使う
       - name: 🏗️ iPhoneアプリ(IPA)をビルド
@@ -73,4 +50,4 @@ jobs:
         with:
           name: DotChain-iOS-IPA
           path: DotChain.ipa
-          retention-days: 7
+          retention-days: 1
diff --git a/app.config.ts b/app.config.ts
index 4bd286c..a8483cd 100644
--- a/app.config.ts
+++ b/app.config.ts
@@ -1,12 +1,12 @@
-import 'dotenv/config';
 import type { ExpoConfig } from '@expo/config';
 
 export default ({ config }: { config: ExpoConfig }) => ({
   ...config,
   extra: {
     ...config.extra,
-    REVENUECAT_IOS_API_KEY: process.env.REVENUECAT_IOS_API_KEY,
-    REVENUECAT_ANDROID_API_KEY: process.env.REVENUECAT_ANDROID_API_KEY,
-    IAP_DEBUG: process.env.IAP_DEBUG ?? process.env.EXPO_PUBLIC_IAP_DEBUG,
+    // iOS Public SDK Key (appl_...) ONLY. Do NOT put Secret API Key here.
+    REVENUECAT_IOS_API_KEY: 'appl_gbzEeaGdycTrUUVKxuZBbToPnEd',
+    // Set to '1' while debugging. Change to '0' (or remove) for release builds.
+    IAP_DEBUG: '1',
   },
 });
